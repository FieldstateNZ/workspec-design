**/dist/**
**/node_modules/**
**/coverage/**
pnpm-lock.yaml

# Vendored verbatim from workspec/docs/design-handoffs — must stay
# byte-identical to the source of record. docs/design/README.md itself is
# authored in this repo (not vendored), so it's carved back out below.
docs/design/**
!docs/design/README.md

# Copied verbatim from the delivery-plan scratchpad source — not reformatted.
DELIVERY_PLAN.md

# @workspec/design committed artifacts — byte content is dictated by
# packages/design/scripts/generate.ts (the drift test in
# packages/design/src/generate/generate.test.ts enforces it), not by
# prettier's CSS/JSON formatter.
packages/design/themes/**
packages/design/tokens.css
packages/design/tailwind.css
packages/design/tokens.json
packages/design/fonts.css

# Generated by packages/design/scripts/build-components-css.ts (real
# @tailwindcss/cli output, not hand-written or prettier-formatted CSS) — the
# drift test in packages/design/src/generate/components-css-drift.test.ts
# enforces its byte content instead.
packages/design/components.css

# Vendored verbatim from workspec/artifacts/workspec/src/styles/design-shell.css
# (S4, workspec-design#5) — must stay byte-identical to that source, like the
# other vendored files below, not reformatted by prettier's CSS printer.
packages/design/design-shell.css

# Generated by packages/design/scripts/contrast-audit.ts — byte content is
# dictated by packages/design/src/contrast/build-audit-report.ts (the drift
# test in packages/design/src/contrast/audit-report.test.ts enforces it),
# not by prettier's markdown-table formatter (which would repad columns
# the generator doesn't).
docs/contrast-audit.md

# Vendored inputs (fonts) and vendored source-of-record CSS — must stay
# byte-identical to what they were copied from. source-of-record/README.md is
# authored in this repo (not vendored), so it's carved back out below.
packages/design/fonts/manifest.json
packages/design/src/tokens/source-of-record/**
!packages/design/src/tokens/source-of-record/README.md

# Playwright e2e artifacts (git-ignored via .gitignore's `test-results/` /
# `playwright-report/`, but git-ignored != prettier-ignored) — without this,
# format:check goes red after any local run of the fixture's e2e suite,
# since Playwright writes `.last-run.json` etc. into test-results/.
# examples/fixture/playwright.config.ts pins `reporter: [['list']]` (no
# 'html'), so it never produces playwright-report/ — only test-results/
# needs ignoring here. apps/preview has no playwright.config, so no
# equivalent artifact dirs to cover there.
examples/fixture/test-results/
