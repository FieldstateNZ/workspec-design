import tokensJson from '@workspec/design/tokens.json';

/**
 * The package's single machine-readable data source. Every section on this
 * page reads from here (or from typed exports built on the same source of
 * truth) — nothing on the page is a hand-authored token list.
 */
export { tokensJson };
